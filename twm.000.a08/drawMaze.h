
#include <windows.h>
#include "maze.h"

/** 
 * this function draws a random maze of CxH 
 * dimensions
 * uses "maze.h" random maze generator
 * draws the maze from the text buffer into the HDC 
 */
INT drawMaze(HDC hdc, int const w, int const h);

